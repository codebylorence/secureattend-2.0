<Window x:Class="BiometricEnrollmentApp.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Clock Out" 
        Height="350" Width="430"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="ScaleTransform.ScaleX" Value="0.98"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,25,0,10">
                <TextBlock Text="🕒" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Confirm Clock Out" FontSize="20" FontWeight="Bold" Foreground="#2D3436" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="30,10">
                <Border Background="#F8F9FA" CornerRadius="10" Padding="20,15" BorderBrush="#EDF2F7" BorderThickness="1">
                    <StackPanel>
                        <TextBlock x:Name="EmployeeNameText" Text="Employee Name" FontSize="16" FontWeight="Bold" Foreground="#2D3436" Margin="0,0,0,2"/>
                        <TextBlock x:Name="EmployeeIdText" Text="Employee ID: 12345" FontSize="12" Foreground="#718096" Margin="0,0,0,10"/>
                        
                        <Separator Background="#E2E8F0" Margin="0,0,0,10"/>
                        
                        <TextBlock x:Name="ClockInTimeText" Text="Clock-in time: 08:00 AM" FontSize="13" Foreground="#4A5568"/>
                        <TextBlock x:Name="WorkingHoursText" Text="Total working time: 8 hours 30 minutes" FontSize="12" Foreground="#48BB78" FontWeight="Medium" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Are you sure you want to end your shift now?" 
                           FontSize="13" Foreground="#718096" TextAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="30,0,30,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton" 
                        Content="Cancel" 
                        Grid.Column="0" Height="45" Margin="0,0,10,0"
                        Background="#EDF2F7" Foreground="#4A5568"
                        Click="CancelButton_Click" 
                        Style="{StaticResource ModernButtonStyle}"/>

                <Button x:Name="ConfirmButton" 
                        Content="✓ Yes, Clock Out" 
                        Grid.Column="1" Height="45"
                        Background="#48BB78" Foreground="White"
                        Click="ConfirmButton_Click" 
                        Style="{StaticResource ModernButtonStyle}"/>
            </Grid>
        </Grid>
    </Border>
</Window>