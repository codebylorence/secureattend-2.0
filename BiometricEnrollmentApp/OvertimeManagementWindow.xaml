<Window x:Class="BiometricEnrollmentApp.OvertimeManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overtime Management"
        Width="800" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#F6F7F9">

    <Window.Resources>
        <!-- Palette -->
        <Color x:Key="PrimaryColor">#1E3A8A</Color>
        <Color x:Key="AccentColor">#2563EB</Color>
        <Color x:Key="MutedText">#6B7280</Color>
        <Color x:Key="CardBg">#FFFFFF</Color>
        <Color x:Key="Surface">#F8FAFC</Color>
        <Color x:Key="OvertimeColor">#7C3AED</Color>
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource MutedText}"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="{StaticResource CardBg}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Surface}"/>
        <SolidColorBrush x:Key="OvertimeBrush" Color="{StaticResource OvertimeColor}"/>

        <!-- Button styles -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="OvertimeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource OvertimeBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBgBrush}" 
                CornerRadius="12" Padding="20" Margin="0,0,0,16"
                BorderBrush="#E6E9EE" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="⏰ Overtime Management" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource OvertimeBrush}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Select employees for overtime work and manage overtime assignments" 
                           FontSize="14" 
                           Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
        </Border>

        <!-- Overtime Assignment Section -->
        <Border Grid.Row="1" Background="{StaticResource CardBgBrush}" 
                CornerRadius="12" Padding="20" Margin="0,0,0,16"
                BorderBrush="#E6E9EE" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Labels -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Employee" 
                           FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Overtime Reason" 
                           FontWeight="SemiBold" Margin="8,0,0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Estimated Hours" 
                           FontWeight="SemiBold" Margin="8,0,0,8"/>

                <!-- Controls -->
                <ComboBox x:Name="EmployeeComboBox" Grid.Row="1" Grid.Column="0"
                          DisplayMemberPath="DisplayName"
                          SelectedValuePath="EmployeeId"
                          Height="34" Margin="0,0,8,0"/>
                
                <ComboBox x:Name="ReasonComboBox" Grid.Row="1" Grid.Column="1"
                          Height="34" Margin="0,0,8,0">
                    <ComboBoxItem Content="Production Rush"/>
                    <ComboBoxItem Content="Project Deadline"/>
                    <ComboBoxItem Content="Staff Shortage"/>
                    <ComboBoxItem Content="Emergency Work"/>
                    <ComboBoxItem Content="Special Assignment"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
                
                <TextBox x:Name="HoursTextBox" Grid.Row="1" Grid.Column="2"
                         Text="2.0" Height="34" Margin="0,0,8,0"
                         VerticalContentAlignment="Center"/>
                
                <Button x:Name="AssignOvertimeBtn" Grid.Row="1" Grid.Column="3"
                        Content="⏰ Assign Overtime"
                        Click="AssignOvertimeBtn_Click"
                        Style="{StaticResource OvertimeButton}"
                        Height="34"/>
            </Grid>
        </Border>

        <!-- Current Overtime Assignments -->
        <Border Grid.Row="2" Background="{StaticResource CardBgBrush}" 
                CornerRadius="12" Padding="20"
                BorderBrush="#E6E9EE" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="📋 Today's Overtime Assignments" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="{StaticResource OvertimeBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Current overtime assignments for today" 
                               FontSize="13" 
                               Foreground="{StaticResource MutedBrush}"/>
                </StackPanel>

                <DataGrid x:Name="OvertimeGrid" Grid.Row="1" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="White"
                          BorderBrush="#e5e7eb"
                          BorderThickness="1"
                          RowHeight="40"
                          ColumnHeaderHeight="44"
                          AlternatingRowBackground="#f9fafb"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId}" Width="120"/>
                        <DataGridTextColumn Header="Employee Name" Binding="{Binding EmployeeName}" Width="*"/>
                        <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="150"/>
                        <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="150"/>
                        <DataGridTextColumn Header="Est. Hours" Binding="{Binding EstimatedHours}" Width="100"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" 
                                            Click="RemoveOvertimeBtn_Click"
                                            Tag="{Binding EmployeeId}"
                                            Background="#EF4444" 
                                            Foreground="White"
                                            FontSize="11"
                                            Padding="8,4"
                                            BorderThickness="0"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#f1f5f9"/>
                            <Setter Property="Foreground" Value="#1e293b"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,6"/>
                            <Setter Property="Foreground" Value="#1f2937"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="RefreshBtn" Content="🔄 Refresh" 
                    Click="RefreshBtn_Click" 
                    Style="{StaticResource {x:Type Button}}"
                    Margin="0,0,8,0"/>
            <Button x:Name="CloseBtn" Content="Close" 
                    Click="CloseBtn_Click" 
                    Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>