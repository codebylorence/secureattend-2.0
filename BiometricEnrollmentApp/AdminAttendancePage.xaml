<Page x:Class="BiometricEnrollmentApp.AdminAttendancePage" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      Title="Admin - Attendance Logs"
      Background="#F6F7F9"
      FontFamily="Segoe UI">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0078D7"/>
        <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="#666666"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DDDDDD"/>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="StatusBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Present">
                    <Setter Property="Background" Value="#E6FFFA"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Late">
                    <Setter Property="Background" Value="#FFFAF0"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Absent">
                    <Setter Property="Background" Value="#FFF5F5"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Overtime">
                    <Setter Property="Background" Value="#EBF8FF"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Present">
                    <Setter Property="Foreground" Value="#047857"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Late">
                    <Setter Property="Foreground" Value="#C05621"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Absent">
                    <Setter Property="Foreground" Value="#C53030"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Overtime">
                    <Setter Property="Foreground" Value="#2B6CB0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Attendance Management" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="#1E3A8A"
                           Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="View and export attendance records" 
                               FontSize="14" 
                               Foreground="#6B7280"
                               Margin="0,0,10,0"/>
                    <TextBlock Text="• Automatic absent marking runs every 5 minutes" 
                               FontSize="12" 
                               Foreground="#10B981"
                               FontStyle="Italic"/>
                </StackPanel>
            </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource LightTextBrush}"/>
                <DatePicker x:Name="FromDatePicker" SelectedDate="{x:Static sys:DateTime.Today}" Width="120" Margin="0,0,15,0"/>
                
                <TextBlock Text="To" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource LightTextBrush}"/>
                <DatePicker x:Name="ToDatePicker" SelectedDate="{x:Static sys:DateTime.Today}" Width="120" Margin="0,0,15,0"/>
                
                <Button x:Name="FilterBtn" Content="Apply Filter" Click="FilterBtn_Click" Style="{StaticResource PrimaryButton}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0">
                <Button x:Name="TodayBtn" Content="Today" Click="TodayBtn_Click"/>
                <Button x:Name="YesterdayBtn" Content="Yesterday" Click="YesterdayBtn_Click"/>
                <Button x:Name="ThisWeekBtn" Content="This Week" Click="ThisWeekBtn_Click"/>
                <Button x:Name="ThisMonthBtn" Content="This Month" Click="ThisMonthBtn_Click"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="RefreshBtn" Content="🔄 Refresh" Click="RefreshBtn_Click" Style="{StaticResource PrimaryButton}"/>
                <Button x:Name="ExportBtn" Content="📥 Export CSV" Click="ExportBtn_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" 
                  x:Name="AttendanceGrid"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#EEEEEE"
                  VerticalGridLinesBrush="Transparent"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="{StaticResource BorderBrush}"
                  RowHeight="45"
                  ColumnHeaderHeight="40"
                  FontSize="13">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#EEEEEE"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" Foreground="#888888"/>
                <DataGridTextColumn Header="Emp ID" Binding="{Binding EmployeeId}" Width="100" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Employee Name" Binding="{Binding EmployeeName}" Width="200"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                
                <DataGridTextColumn Header="In" Binding="{Binding ClockIn}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#059669"/> </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Out" Binding="{Binding ClockOut}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#DC2626"/> </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Hours" Binding="{Binding TotalHours}" Width="80" FontWeight="SemiBold"/>

                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StatusBorderStyle}">
                                <TextBlock Text="{Binding Status}" Style="{StaticResource StatusTextStyle}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>