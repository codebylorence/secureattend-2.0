<Page x:Class="BiometricEnrollmentApp.AttendancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#f7f9fc">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <TextBlock Text="📋 Attendance" FontSize="24" FontWeight="Bold" Foreground="#1e293b"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,12">
            <Button x:Name="GoEnrollBtn"
                    Content="🖐️ Go to Enrollment"
                    Width="160" Height="40"
                    Background="#10b981" Foreground="White"
                    FontWeight="SemiBold"
                    Click="GoEnrollBtn_Click"/>
            <Button x:Name="SimulateScanBtn"
                    Content="🔎 Simulate Scan"
                    Width="140" Height="40"
                    Margin="8,0,0,0"
                    Click="SimulateScanBtn_Click"/>
        </StackPanel>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16" BorderBrush="#e2e8f0" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Attendance Log" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <ListBox x:Name="AttendanceLog" Height="420"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3" Text="© 2025 SecureAttend Systems" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#94a3b8"/>
    </Grid>
</Page>
